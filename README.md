# Grubhub(GraphQL)

### Open Backend, run command "npm install"
### Open Frontend, run command "npm install"

### To run Backend,
* run command "nodemon"

### To run Frontend
* run command "npm start"
